using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeavenEnemy1 : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        SetCharacterName("HeavenEnemy");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
